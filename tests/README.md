# Tests directory — PDF Swiss-Knife

Test fixtures (sample PDFs) go in `fixtures/`.

Run tests with:

    cd "f:\PDF Swiss-Knife"
    pytest tests/ -v

Tests require the backend dependencies to be installed:

    pip install -r requirements.txt

Individual test modules will be created as each feature milestone completes.
